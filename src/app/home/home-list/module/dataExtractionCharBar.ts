import { ConvertDateTime } from 'src/app/home/home-list/module/ConvertDateTime';
export class dataExtractionCharBar{
  convertDateTime = new ConvertDateTime();
  lables: any=[];

  CreateLables(request: any): any{
    for(var i in request){

    }
  }
}
